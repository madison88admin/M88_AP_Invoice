{
  "name": "ap-invoice-automation",
  "version": "1.0.0",
  "private": true,
  "description": "AP Invoice Processing Automation System for Madison 88",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:generate": "cd packages/db && npx prisma generate",
    "db:push": "cd packages/db && npx prisma db push",
    "db:seed": "cd packages/db && npx prisma db seed",
    "db:migrate": "cd packages/db && npx prisma migrate dev"
  },
  "devDependencies": {
    "turbo": "^1.11.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
